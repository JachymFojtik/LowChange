using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using LowChange;

namespace LowChange_Test
{
    [TestClass]
    public class CountTest
    {
        [TestMethod]
        public void LowestChange_Test()
        {
            LowestChange(500, 449);
            Assert.IsTrue(coinCount[50] = 1 && coinCount[1] = 1);
        }
    }
}
